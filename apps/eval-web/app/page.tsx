export default function HomePage() {
    return (
        <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
            <h1>Model Eval</h1>
            <p>
                Run an image or text dataset through several candidate models and
                compare structured output, latency, cost, and scores.
            </p>
            <p style={{ color: "#888" }}>
                Scaffold — datasets, runs, matrix, and leaderboard surfaces are
                implemented in later units (U5–U10).
            </p>
        </main>
    );
}
